# Meson out-of-tree build directories
/_build/
/build/
builddir/

# Rust / Cargo
/target/
# Cargo.lock is intentionally committed for reproducible builds (binary crate).

# Generated by Meson's configure_file() and copied into src/ for Cargo
/src/config.rs
build-aux/signing.conf

# Build artefacts
/*.deb

# Flatpak
/_flatpak-build/
/.flatpak-builder/
/cargo-sources.json
